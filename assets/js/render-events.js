document.querySelectorAll('[data-events]').forEach(el=>{el.innerHTML=(window.FACIG_EVENTS||[]).map(e=>`<article class="event-card"><span class="eyebrow">${e.date}</span><h3>${e.title}</h3><p><strong>${e.place}</strong> — ${e.text}</p></article>`).join('')||'<p>Les prochains rendez-vous seront annoncés ici.</p>'});
