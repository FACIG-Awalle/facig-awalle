document.querySelectorAll('[data-events]').forEach(el=>{el.innerHTML=(window.FACIG_EVENTS||[]).map(e=>`<article class="event-card">${e.image?`<a href="${e.image}" target="_blank" rel="noopener"><img src="${e.image}" alt="Flyer ${e.title}" style="display:block;width:100%;max-width:430px;height:auto;margin:0 auto 22px;border-radius:18px;box-shadow:0 8px 24px rgba(0,0,0,.12);"></a>`:''}<span class="eyebrow">${e.date}</span><h3>${e.title}</h3><p><strong>${e.place}</strong> — ${e.text}</p></article>`).join('')||'<p>Les prochains rendez-vous seront annoncés ici.</p>'});
